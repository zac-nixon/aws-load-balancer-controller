---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: targetgroupconfigurations.nlbgateway.k8s.nlb.aws
spec:
  group: nlbgateway.k8s.nlb.aws
  names:
    kind: TargetGroupConfiguration
    listKind: TargetGroupConfigurationList
    plural: targetgroupconfigurations
    singular: targetgroupconfiguration
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: TargetGroupConfiguration is the Schema for defining TargetGroups
          with an AWS ELB Gateway
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetGroupConfigurationSpec defines the TargetGroup properties
              for a route.
            properties:
              enableProxyProtocol:
                description: enableProxyProtocol ....
                type: boolean
              healthCheckConfig:
                description: healthCheckConfig The Health Check configuration for
                  this backend.
                properties:
                  healthCheckCodes:
                    description: healthCheckCodes ....
                    items:
                      type: string
                    type: array
                  healthCheckHealthyThreshold:
                    description: healthCheckHealthyThreshold ....
                    format: int32
                    type: integer
                  healthCheckInterval:
                    description: healthCheckInterval ....
                    format: int32
                    type: integer
                  healthCheckPath:
                    description: healthCheckPath ....
                    type: string
                  healthCheckPort:
                    description: healthCheckPort ....
                    type: string
                  healthCheckProtocol:
                    description: healthCheckProtocol ....
                    type: string
                  healthCheckTimeout:
                    description: healthCheckTimeout ....
                    format: int32
                    type: integer
                  healthCheckUnHealthyThreshold:
                    description: healthCheckUnHealthyThreshold ....
                    format: int32
                    type: integer
                type: object
              instanceNodeSelector:
                additionalProperties:
                  type: string
                description: nodeSelector The node selector used for instance type
                  target groups.
                type: object
              loadBalancerSourceRanges:
                description: LoadBalancerSourceRanges restricts traffic through the
                  load-balancer to only the specified client IPs.
                items:
                  type: string
                type: array
              manageBackendSecurityGroupRules:
                description: manageBackendSecurityGroupRules flag to enable / disable
                  the addition of rules to the cluster SG. The default is true.
                type: boolean
              targetGroupAttributes:
                additionalProperties:
                  type: string
                description: targetGroupAttributes ....
                type: object
              targetReference:
                description: targetReference the kubernetes object to attach the Target
                  Group settings to.
                properties:
                  group:
                    default: ""
                    description: |-
                      Group is the group of the referent. For example, "gateway.networking.k8s.io".
                      When unspecified or empty string, core API group is inferred.
                    type: string
                  kind:
                    default: Service
                    description: |-
                      Kind is the Kubernetes resource kind of the referent. For example
                      "Service".


                      Defaults to "Service" when not specified.


                      ExternalName services can refer to CNAME DNS records that may live
                      outside of the cluster and as such are difficult to reason about in
                      terms of conformance. They also may not be safe to forward to (see
                      CVE-2021-25740 for more information). Implementations SHOULD NOT
                      support ExternalName Services.


                      Support: Core (Services with a type other than ExternalName)


                      Support: Implementation-specific (Services with type ExternalName)
                    type: string
                  name:
                    description: Name is the name of the referent.
                    type: string
                required:
                - name
                type: object
              targetType:
                description: |-
                  TODO -- ADD ENUMS!
                  targetType defaults to instance.
                type: string
            required:
            - targetReference
            type: object
          status:
            description: TargetGroupConfigurationStatus defines the observed state
              of TargetGroupConfiguration
            properties:
              observedGatewayClassConfigurationGeneration:
                description: The generation of the Gateway Configuration attached
                  to the GatewayClass object.
                format: int64
                type: integer
              observedGatewayConfigurationGeneration:
                description: The generation of the Gateway Configuration attached
                  to the Gateway object.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
