---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nlbgatewayconfigurations.gateway.k8s.aws
spec:
  group: gateway.k8s.aws
  names:
    kind: NLBGatewayConfiguration
    listKind: NLBGatewayConfigurationList
    plural: nlbgatewayconfigurations
    singular: nlbgatewayconfiguration
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: NLBGatewayConfiguration is the Schema for the NLBGatewayConfiguration
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NLBGatewayConfigurationSpec defines the desired state of
              NLBGatewayConfiguration
            properties:
              MultiClusterEnabled:
                description: multiClusterEnabled optional boolean that toggles multi
                  cluster capabilities
                type: boolean
              accessLogConfiguration:
                description: |-
                  DONE
                  loadBalancerAttributes optional access log configuration for the load balancer.
                properties:
                  accessLogsEnabled:
                    description: accessLogsEnabled whether or not that access logs
                      should be turned sent.
                    type: boolean
                  bucketName:
                    description: bucketName the bucket to send access logs to.
                    type: string
                  prefix:
                    description: prefix is the prefix added to the bucket name.
                    type: string
                type: object
              enableBackendSecurityGroupRules:
                description: |-
                  DONE
                  enableBackendSecurityGroupRules an optional boolean flag that controls whether the controller should automatically add the ingress rules to the instance/ENI security group.
                type: boolean
              enableCrossZoneLoadBalancing:
                description: |-
                  DONE
                  crossZoneLoadBalancingEnabled optional boolean that toggles routing across availability zones.
                type: boolean
              extraResourceTags:
                additionalProperties:
                  type: string
                description: |-
                  DONE
                  extraResourceTags an optional map of additional tags to apply to AWS resources.
                type: object
              listenerAttributes:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  DONE
                  listenerAttributes an optional map of attributes that maps listeners protocol + port to their attributes
                type: object
              loadBalancerAttributes:
                additionalProperties:
                  type: string
                description: |-
                  DONE
                  loadBalancerAttributes an optional map of attributes to apply to the load balancer.
                type: object
              loadBalancerIPType:
                description: |-
                  DONE
                  loadBalancerIPType defines what kind of load balancer to provision (ipv4, dual stack)
                enum:
                - ipv4
                - dualstack
                type: string
              loadBalancerName:
                description: |-
                  DONE
                  loadBalancerName defines the name of the NLB to provision. If unspecified, it will be automatically generated.
                maxLength: 32
                minLength: 1
                type: string
              loadBalancerScheme:
                description: |-
                  DONE
                  loadBalancerScheme defines the type of NLB to provision. If unspecified, it will be automatically inferred.
                enum:
                - internal
                - internet-facing
                type: string
              loadBalancerSecurityGroupPrefixes:
                description: |-
                  DONE
                  loadBalancerSecurityGroupPrefixes an optional list of prefixes that are allowed to access the NLB.
                items:
                  type: string
                type: array
              loadBalancerSecurityGroups:
                description: |-
                  DONE
                  loadBalancerSecurityGroups an optional list of security group ids or names to apply to the NLB
                items:
                  type: string
                type: array
              loadBalancerSourceRanges:
                description: |-
                  DONE - But this is defined in the TG config as well.
                  loadBalancerSourceRanges an optional list of CIDRs that are allowed to access the NLB.
                items:
                  type: string
                type: array
              loadBalancerSubnets:
                description: |-
                  DONE
                  loadBalancerSubnets an optional list of subnet ids or names to be used in the NLB
                items:
                  description: SubnetConfiguration defines the subnet settings for
                    a Load Balancer.
                  properties:
                    eipAllocation:
                      description: eipAllocation the EIP name for this subnet.
                      type: string
                    identifier:
                      description: identifier name or id for the subnet
                      type: string
                    privateIPv4Allocation:
                      description: privateIPv4Allocation the private ipv4 address
                        to assign to this subnet.
                      type: string
                    privateIPv6Allocation:
                      description: privateIPv6Allocation the private ipv6 address
                        to assign to this subnet.
                      type: string
                  required:
                  - identifier
                  type: object
                type: array
              sslConfiguration:
                additionalProperties:
                  description: SSLConfiguration defines the SSL configuration for
                    a listener on the Load Balancer.
                  properties:
                    alpnPolicy:
                      description: alpnPolicy an optional string that allows you to
                        configure ALPN policies on your Load Balancer
                      enum:
                      - HTTP1Only
                      - HTTP2Only
                      - HTTP2Optional
                      - HTTP2Preferred
                      - None
                      type: string
                    backendProtocol:
                      description: backendProtocol the protocol to use...
                      type: string
                    certificates:
                      description: certificates the list of other certificates to
                        add to the listener.
                      items:
                        type: string
                      type: array
                    defaultCertificate:
                      description: defaultCertificate the cert arn to be used by default.
                      type: string
                    negotiationPolicy:
                      description: negotiationPolicy the policy to use...
                      type: string
                  required:
                  - defaultCertificate
                  type: object
                description: |-
                  DONE
                  sslConfiguration an optional map that maps listener port to TLS settings.
                type: object
            type: object
          status:
            description: NLBGatewayConfigurationStatus defines the observed state
              of TargetGroupBinding
            properties:
              observedGatewayClassConfigurationGeneration:
                description: The generation of the Gateway Configuration attached
                  to the GatewayClass object.
                format: int64
                type: integer
              observedGatewayConfigurationGeneration:
                description: The generation of the Gateway Configuration attached
                  to the Gateway object.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
