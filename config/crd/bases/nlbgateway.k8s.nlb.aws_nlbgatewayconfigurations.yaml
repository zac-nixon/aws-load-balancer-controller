---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nlbgatewayconfigurations.nlbgateway.k8s.nlb.aws
spec:
  group: nlbgateway.k8s.nlb.aws
  names:
    kind: NLBGatewayConfiguration
    listKind: NLBGatewayConfigurationList
    plural: nlbgatewayconfigurations
    singular: nlbgatewayconfiguration
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: NLBGatewayConfiguration is the Schema for the NLBGatewayConfiguration
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NLBGatewayConfigurationSpec defines the desired state of
              NLBGatewayConfiguration
            properties:
              loadBalancerScheme:
                description: loadBalancerScheme defines the type of NLB to provision.
                  If unspecified, it will be automatically inferred.
                enum:
                - internal
                - internet-facing
                type: string
              loadBalancerSubnets:
                description: loadBalancerSubnets an optional list of subnet ids or
                  names to be used in the NLB
                items:
                  type: string
                type: array
            type: object
          status:
            description: NLBGatewayConfigurationStatus defines the observed state
              of TargetGroupBinding
            properties:
              observedGatewayClassConfigurationGeneration:
                description: The generation of the Gateway Configuration attached
                  to the GatewayClass object.
                format: int64
                type: integer
              observedGatewayConfigurationGeneration:
                description: The generation of the Gateway Configuration attached
                  to the Gateway object.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
